The following things needs fixed:

* Clear the tool - ie reset the state atom DONE
* Fix the postcode input form (on-submit rather than on-change) DONE

The following improvements can be made:

* Content markdown should be editable and persisted to a database somewhere
* Postcode data should be validated
* Error responses in logical places
* Animation to show activity
